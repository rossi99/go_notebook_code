package main

import "fmt"

func main() {
	fmt.Println("Garbage Collection Points")

	// Garbage Collector (GC) gets involved when value is constructed on heap. Pacing algorithm is the most important part
	// of GC <- determines the pace that GC must run to maintain heap, should be the smallest heap possible in conjunction
	// with the best app throughput.
}
